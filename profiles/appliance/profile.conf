# Package truth: profiles/<profile>/packages.txt (+ optional .isdconfig extras).
# INSTALL_* below are legacy fallbacks only; prefer packages.txt.
PROFILE_NAME=appliance
USERLAND_BASE=busybox
INIT_SYSTEM=runit
LOGIN_POLICY=none
ROOT_POLICY=locked
ROOT_AUTOLOGIN=0
NETWORK_MODE=none
INSTALL_SMOKE=0
FSCK_ON_BOOT=0
