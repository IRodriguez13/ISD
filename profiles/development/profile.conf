# Lab profile — NOT FOR PRODUCTION.
# Package truth: profiles/development/packages.txt (+ optional .isdconfig extras).
# INSTALL_* below are legacy fallbacks only; prefer packages.txt.
PROFILE_NAME=development
USERLAND_BASE=busybox
INIT_SYSTEM=runit
LOGIN_POLICY=autologin_root
ROOT_POLICY=empty_password_lab
ROOT_AUTOLOGIN=1
NETWORK_MODE=none
INSTALL_SMOKE=1
FSCK_ON_BOOT=1
WARNING=NOT_FOR_PRODUCTION
